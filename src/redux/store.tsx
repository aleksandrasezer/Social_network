
export type PostType = {
    id: string
    postText: string
    likesCount: number
}
export type MessageType = {
    id: string
    messageText: string
}
export type DialogType = {
    id: string
    name: string
}

